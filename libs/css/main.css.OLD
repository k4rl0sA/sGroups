
  /*=========ZONE FILTERS AND DATA=============*/
.wrapper{
    max-width: 100%;
    width: 90%;
    height: max-content;
    padding: 15px 20px;
    border-radius: 5px;
    overflow: hidden;
    background: var(--primary-color-light);
    box-shadow: 0px 5px 15px rgba(0,0,0,0.1);
  }

  .top-menu{
    background:#0c63cb;
    border-radius: 14px 14px 0 0;
  }
   
  .wrapper header{
    font-size: 30px;
    font-weight: 600;
    padding-bottom: 20px;
  }
  .wrapper nav{
    position: relative;
    width: 50%;
    display: flex;
    align-items: center;
    left:8px;
  }

  .wrapper nav label{
    font-family: "Merienda", cursive;
    display: block;
    height: 100%;
    width: 100%;
    text-align: center;
    line-height: 50px;
    cursor: pointer;
    position: relative;
    top:8px;
    z-index: 1;
    color: var(--color-icons);
    font-size: 17px;
    border-radius: 5px;
    margin: 0 5px;
    transition: all 0.3s ease;
  }
  /*.wrapper nav label:hover{
     background: var(--body-color);
    color: var(--text-color); 
  }*/
  #filtros:checked ~ nav label.filtros,
  #datos:checked ~ nav label.datos{
    color: var(--color-icons);
  }
  nav label i{
    padding-right: 7px;
  }
  nav .slider{
    position: absolute;
    height: 100%;
    width: 50%;
    top:8px;
    left: 0;
    bottom: 0;
    z-index: 0;
    border-radius: 5px;
    background: var(--primary-color);
    transition: all 0.3s ease;
  }
  input[type="radio"]{
    display: none;
  }
  #datos:checked ~ nav .slider{
    left: 50%;
  }
  section .content{
    display: none;
    background: var(--primary-color-light);
    color: var(--text-color);
  }
  #filtros:checked ~ section .content-1,
  #datos:checked ~ section .content-2{
    display: block;
  }
.sidebar.close .wrapper nav label{
    font-size: 10px;
}
  section .content .title{
    font-family: "Poiret One", sans-serif;
    font-size: 21px;
    font-weight: 500;
    margin: 15px 0 10px 0;
  }
  section .content p{
  text-align: justify;
  }
  /*======================*/
  /*=========TEXTO=============*/
  .poppins-font {
    font-family: 'Poppins', sans-serif;
}

.merienda-font {
    font-family: 'Merienda', cursive;
}

  .txt-center {
    text-align: center;
  }
  .txt-left {
    text-align: left;
  }
  .txt-right {
    text-align: right;
  }
  .txt-upper {
    text-transform: uppercase;
  }
  .txt-lower {
    text-transform:lowercase;
  }
  .txt-capit {
    text-transform:capitalize;
  }
/*===========ACCESS LOCK=================*/
.lock{
  text-align: center;
  font-size: 16px;
  color:var(--text-color);
}

.lock-icon {
  color: var(--error-color);
  margin-bottom: 20px;
}

h2 {
  font-size: 32px;
  margin-bottom: 10px;
  color: var(--text-color);
}
/*=============================*/
/*===========FORMS=================*/ 
.input-box {
  position: relative;
  width: 100%;
  margin-bottom: 20px;
}

.input-box input[type="text"],
.input-box input[type="number"],
.input-box input[type="date"],
.input-box select,
.input-box textarea {
  padding: 15px 15px 3px 15px;
  border-radius: 5px;
  border: none;
  border-bottom: 2px solid #ccc;
  background-color: transparent;
  font-size: 16px;
  outline: none;
  transition: border-bottom var(--tran-03);
}

.input-box label {
  text-transform: capitalize;
  position: absolute;
  left: 10px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-color);
  font-size: 16px;
  pointer-events: none;
  transition: all var(--tran-03);
}

.input-box select + label {
  left: 8em;
  top: 61%;
}

.input-box input:focus ~ label,
.input-box input:disabled ~ label,
.input-box input:valid ~ label{
  text-transform: capitalize;
  top: -5px;
  font-size: 16px;
  color: var(--primary-color);
}

.input-box select:focus ~ label,
.input-box select:disabled ~ label,
.input-box select:valid ~ label{
  text-transform: capitalize;
  top: -5px;
  left: 10px;
  font-size: 16px;
  color: var(--primary-color);
}

.input-box textarea:focus ~ label,
.input-box textarea:valid ~ label {
  text-transform: capitalize;
  top: -5px;
  font-size: 16px;
  color:var(--primary-color);
}

.input-box input:not(:placeholder-shown),
.input-box select:not(:placeholder-shown),
.input-box textarea:not(:placeholder-shown) {
  border-bottom: 3px solid var(--primary-color);
}

.input-box input:not(:focus):not(:disabled),
.input-box select:not(:focus):not(:disabled),
.input-box textarea:not(:focus):not(:disabled) {
  border-bottom: 3px solid var(--primary-color);
  animation: fillBorder 1s ease forwards;
}




@keyframes fillBorder {
  0% {
    width: 0;
    right: 50%;
    transform-origin: right;
  }
  100% {
    width: 100%;
    right: 0;
  }
}

.form-cmp {
  width: 90%;
  margin: 0 auto;
  padding: 20px;
  border-radius: 5px;
  background-color: var(--body-color);
  box-shadow: 0 0 25px rgba(0, 0, 0, 0.1);
}

.btn {
  width: 100%;
  height: 45px;
  background: var(--primary-color);
  border: none;
  border-radius: 40px;
  cursor: pointer;
  font-size: 16px;
  color: #fff;
  font-weight: 600;
  z-index: 1;
  overflow: hidden;
  position: relative;
  transition: background-color var(--tran-03);
}

.btn::before {
  content: '';
  position: absolute;
  top: -100%;
  left: 0;
  width: 100%;
  height: 300%;
  background: linear-gradient(var(--primary-color), var(--secundary-color));
  z-index: -1;
  transition: .5s;
}

.btn:hover::before {
  top: 0;
}

.btn:hover {
  background-color: var(--secundary-color);
}

button.btn[disabled] {
  background-color: #ccc;
  cursor: not-allowed;
}
 
.error {
  padding: 20px;
  background-color: var(--error-color);
  color: white;
  border-radius: 25px;
  position: absolute;
  z-index: 100;
  top: 0;
}

.ok {
  padding: 20px;
  background-color: #00f372d6;
  color: white;
  border-radius: 25px;
  position: absolute;
  z-index: 100;
  top: 0;
}

/*==========FORMULARIOS===================*/
.col-1 {flex:0 0 10%;}.col-15 {flex:0 0 15%;}.col-2 {flex:0 0 20%;}.col-25 {flex:0 0 25%;}.col-10 {flex:0 0 20%;height:20vh;}.col-3 {flex:0 0 30%;}.col-35 {flex:0 0 35%;}.col-4 {flex:0 0 40%;}.col-45 {flex:0 0 45%;}.col-5 {flex:0 0 50%;}.col-55 {flex:0 0 55%;}.col-6 {flex:0 0 60%;}.col-65 {flex:0 0 65%;}.col-7 {flex:0 0 70%;}.col-75 {flex:0 0 75%;}.col-8 {flex:0 0 80%;}.col-85 {flex:0 0 85%;}.col-9 {flex:0 0 90%;}.col-95 {flex:0 0 95%;}.col-0 {flex:0 0 100%;}
.frm-data{position: absolute; top: 0;left: 0;margin: 0 15px 10px 0;height: 100vh;width:100%;background:var(--body-color);z-index: 2; padding: 10px 10px 10px 10px;}
body, html {
  margin: 0;
  padding: 0;
  height: 100%;
}

.ventana {
  background: #f3f5fa;
  height: 100%;
  display: flex;
  flex-direction: column;
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1); /* Sombra */
  background-color: white;
}

.barra-titulo {
  background-color: #0c63cb;
  color: white;
  padding: 20px;
  text-align: center;
  border-top-left-radius: 10px;
  border-top-right-radius: 10px;
  position: relative;
  box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2); /* Sombra */
}

.btn-cerrar {
  position: absolute;
  top: 50%;
  right: 20px;
  transform: translateY(-50%);
  width: 40px;
  height: 40px;
  background: none;
  border: none;
  cursor: pointer;
}

.btn-cerrar i {
  color: white;
  font-size: 24px;
}

.frm-menu{
  background-color: #dbdbdb;
  padding: 20px;
  text-align: center;
  border-bottom-left-radius: 10px;
  border-bottom-right-radius: 10px;
  box-shadow: 0 -5px 10px rgba(0, 0, 0, 0.2); /* Sombra */
}

.frm-btn {
  padding: 10px 20px;
  margin: 0 10px;
  font-size: 16px;
  color: white;
  background-color: #0056b3;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease; /* Transición de color */
}

.frm-btn:hover {
  background-color: #004080;
}
.frm-row{
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 10px;
  padding: 15px 7px 10px 10px;
  border-style: inset inset hidden inset;
  border-left: 2px inset;
  border-right: 1px solid;
  margin-bottom: 1rem
}

.frm-title{
  font-family: "Poiret One", sans-serif;
  font-size: 32px;
  font-weight: 900;
  color: var(--sidebar-color);
  text-transform: capitalize;
}

[class^="col-"] {
  flex: 1;
}

nav.menu.right li {
  cursor: pointer;
}

li {
  list-style: none;
  display: inline-block;
}

.row {
  display: -ms-flexbox;
  display: flex;
  -ms-flex-wrap: wrap;
  flex-wrap: wrap;
  margin-right: -15px;
  margin-left: -15px;
}

.form-group {
  margin-bottom: 1rem;
}

    /****************** TABLA ************************************/

    .material-icons{
      color: var(--toggle-color);
    }
    
    .material-icons.s9 { font-size:0.563em;}
    .material-icons.s13 { font-size:0.813em;}
    .material-icons.s18 { font-size:1.125em;}
    .material-icons.s24 { font-size:1.5em;}
    .material-icons.s36 { font-size: 2.25em;}
    .material-icons.s48 { font-size: 3em;}
    
    .datatable tbody{
      font-size:var(--font-size-body);
      background-color: var(--sidebar-color);
    }
    
    .datatable-container{
      font-family: var(--font-base);
      /* background-color: var(--body-color); */
      border-radius: var(--dt-border-radius);
      color: var(--dt-text-col);
      width: 100%;
      margin: 0 auto;
      font-size: var(--font-size-tit);
    }
    
    .datatable-container select{
      display: inline;
      width: auto;
    }
    
    .datatable-container thead{
    color: var(--color-icons); 
    background: var(--secundary-color);
    font-size: 1.1rem;
    text-transform: uppercase;
    }
    
    thead tr {
      border-bottom: solid 0.0625em var(--dt-borde-col);
    }

    tbody tr{
      /* border-bottom: solid 0.0625em var(--dt-separa-row-color); */
    }
    
    .datatable-container .header-tools{
    border-bottom: solid 0.0625em var(--dt-borde-col);
    padding: var(--dt-padding);
    display: flex;
    align-items: baseline;
    }
    
    .datatable-container .header-tools .search .search-input{
      width: 100%;
      background-color: var(--dt-bg-act-btn);
      display: block;
      box-sizing: border-box;
      border-radius: var(--dt-border-radius);
      border:solid 0.0625em var(--dt-borde-col) ;
      color: var(--dt-borde-col);
    }
    
    .datatable-container .header-tools .tools{
      width:100%;
    }
    
    .datatable-container .header-tools .tools ul{
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: start;
      align-items: baseline;
    }
    
    .datatable-container .header-tools .tools ul li{
      display: inline-block;
      margin: 0 var(--dt-padding-xs);
      align-items: baseline;
    }
    
    .datatable-container .footer-tools{
      padding: var(--dt-padding);
      display: flex;
      align-items: baseline;
    }
    
    .datatable-container .footer-tools .list-items{
      width: 50%;
    }
    
    .datatable-container .footer-tools .pages{
        margin-left: auto;
        margin-right: 0;
        /* width: 50%; */
    }
    
    .datatable-container .footer-tools .pages ul{
      margin: 0;
      padding: 0;
      display: flex;
      align-items: baseline;
      justify-content: flex-end;
    }
    
    .datatable-container .footer-tools .pages ul li{
      display: inline-block;
      margin: 0 var(--dt-padding-xs);
    }
    
    .datatable-container .footer-tools .pages ul li button,
    .datatable-container .header-tools .tools ul li button{
      color: var(--dt-text-col-btn);
      width: 100%;
      box-sizing: border-box;
      border: 0;
      border-radius: 100%;
      background: transparent;
      cursor: pointer;
      padding: var(--dt-padding-s) var(--dt-padding);
    }
    
    /* .datatable-container .footer-tools .pages ul li button:hover,
    .datatable-container .header-tools .tools ul li button:hover{
      background:var(--dt-bg-act-btn);
      color:var(--dt-text-col-act-btn);
    } */
    .pages input{
      display: inline;
    }
    
    .pagin button {
      color: var(--dt-text-col-btn);
        width: 100%;
        box-sizing: border-box;
        border: 0;
        border-radius: 100%;
        background: transparent;
        cursor: pointer;
        padding: var(--dt-padding-s) var(--dt-padding);
    }
    
    .circ:hover {
      background: #af8e8e82;
      /* color: var(--dt-text-col-act-btn); */
      border-radius: 50%;
    }
    
    
    
    .datatable-container .footer-tools .pages ul li span.active{
      background-color: var(--color_text_menu);
      border-radius: var(--dt-border-radius);
    }
    
    .datatable-container .footer-tools .pages ul li button,
    .datatable-container .footer-tools .pages ul li span,
    .datatable-container .header-tools .tools ul li button{
    padding: var(--dt-padding-s) var(--dt-padding);
    }
    
    .datatable-container .datatable{
      border-collapse: collapse;
      width: 100%;
      color: var(--color_text_menu);
    }
    
    .datatable-container .datatable,
    .datatable-container .datatable th,
    .datatable-container .datatable td{
    padding: var(--dt-padding-s) var(--dt-padding-s);
    }
    
    .datatable-container .datatable th{
      font-weight: bolder;
      text-align: center;
      /* border-bottom: solid 0.0625em var(--dt-borde-col); */
    }
    
    .datatable-container .datatable td{
      /* border-bottom: solid 0.0625em var(--dt-borde-col); */
    }
    
    .datatable-container .datatable tbody tr:nth-child(even){
      /* background-color:var(--dt-even-row-col); */
      /* background: var(--backgr-color-head); */
      /* color: var(--color-icons); */
    }
    .datatable-container .datatable tbody tr:nth-child(odd){
      /* background-color:var(--primary-color-light); */
      /* color:rgb(61,68,73); */
    }
    .datatable-container .datatable tbody tr:hover{
      background-color: var(--dt-hover-cel-col);
      color:var(--color-icons) ;
    }
    
    .datatable-container .datatable tbody tr .A::after,
    .datatable-container .datatable tbody tr .away::after,
    .datatable-container .datatable tbody tr .I::after{
        display: inline-block;
        vertical-align: middle;
    }
    
    .datatable-container .datatable tbody tr .A::after{
        content: 'Activo';
        color:var(--dt-status-A-color);
    }
    .datatable-container .datatable tbody tr .I::after{
      content: 'Inactivo';
      color:var(--dt-status-I-color);
    }
    .datatable-container .datatable tbody tr .P::after{
      content: 'Pendiente';
      color:var(--dt-status-P-color);
    }
    .datatable-container .datatable tbody tr .A::before,
    .datatable-container .datatable tbody tr .P::before,
    .datatable-container .datatable tbody tr .I::before{
        content: '';
        display: inline-block;
        width:0.625em;
        height:0.625em;
        margin-right:0.625em;
        border-radius: 50%;
        vertical-align: middle;
    }
    
    .datatable-container .datatable tbody tr .A::before{
      background-color:var(--dt-status-A-color);
    }
    .datatable-container .datatable tbody tr .I::before{
      background-color:var(--dt-status-I-color);
    }
    .datatable-container .datatable tbody tr .P::before{
      background-color:var(--dt-status-P-color);
    }


    .datatable-container .resumen .menu li {
      display: inline-block;
      margin-right: 5px; 
      cursor: pointer; 
    }
    .datatable-container .resumen input.pagina {
      width: 30px; 
      margin-right: 5px; 
    }
    .datatable-container .resumen span {
      margin-right: 5px; 
    }
    .datatable-container .resumen span b {
      font-weight: normal; 
    }
    td.resumen {
      background-color: var(--primary-color);
      border-top: solid 0.0825em var(--dt-borde-col);
      border-bottom-left-radius: 25px;
      border-bottom-right-radius: 25px;
      color: var(--color-icons);
      font-size: 1.1rem;
      text-transform: capitalize;
      text-align: center;
      vertical-align: middle;
    }
    .center{float:center}
    .right{float:right;}
    .left{float:left;}
    .btn-centrado{text-align:center;margin:auto;}
    .centro{margin-left: auto;margin-right: auto;}
    
    .resumen .menu {
      /* display: flex;  */
      justify-content: space-between; 
      align-items: center;
  }
  
  .resumen .menu li {
      margin: 0 5px; 
  }
  
  .resumen .menu input {
      margin: 0 5px; 
  }

/**************************************************/    
@media (max-width: 43.75em) {
  .campo{
    width: 100%;
  }
  .inline {
    flex-direction: column;
    align-items: stretch;
  }
.input-box {
      width: 100%;
  }

/*********TABLA MEDIA QUERY*********/
table{
  width: 100%;
}
  table tr{
    display: flex;
    flex-direction:column;
    border: .06em solid var(--secundary-color);
    /* padding: 1em; */
    margin-bottom: 1em;
    font-size: 0.8rem;
  }

  td nav {
    font-size: large;
  }

table thead{
  display: none;
}

.datatable-container .datatable, .datatable-container .datatable th, .datatable-container .datatable td {
  padding: var(--dt-padding-xs) var(--dt-padding-xs);
}

table td[data-tit]{
  display: flex;
}
table td[data-tit]::before{
  content: attr(data-tit);
  width: 6em;
  margin-right: 1em;
}
.pages input{
  display: flex;
}
}



